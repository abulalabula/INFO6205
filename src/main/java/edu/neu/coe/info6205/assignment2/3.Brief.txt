In the quadratic method, I sort the array first, then use two pointers at head(low) and tail(high) to accelerate the finding process of the corresponding three numbers. Because it will decrease the time to traverse the array. 
